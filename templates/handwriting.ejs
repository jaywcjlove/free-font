<%- include('./_partial/header', { type: "home", title: "手绘体", pageurl: "handwriting.html" }); %>
<header>
<%- include('./_partial/header-title', { length: DATA.filter((item) => item.type == "手绘体").length, type: "手绘体" }); %>
</header>
<main>
  <% DATA.filter((item) => item.type == "手绘体").sort((a, b) => a.ctime - b.ctime).forEach(function(font, idx){ %>
    <%- include('./_partial/home-list', { font }); %>
  <% }); %>
</main>
<%- include('./_partial/footer', { }); %>