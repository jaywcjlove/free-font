<%- include('./_partial/header', { type: "home", title: "艺术体", pageurl: "art.html" }); %>
<header>
<%- include('./_partial/header-title', { length: DATA.filter((item) => item.type == "艺术体").length, type: "艺术体" }); %>
</header>
<main>
  <% DATA.filter((item) => item.type == "艺术体").sort((a, b) => a.ctime - b.ctime).forEach(function(font, idx){ %>
    <%- include('./_partial/home-list', { font }); %>
  <% }); %>
</main>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-Z10L1MBTYC"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-Z10L1MBTYC');
</script>
<%- include('./_partial/footer', { }); %>